public enum BuildingCategory
{
    // ── Airside (côté piste) ──────────────────────────────────────────────
    Runway,
    Taxiway,
    Gate,
    Apron,
    ControlTower,
    FuelStation,
    CargoArea,

    // ── Landside (côté terminal) ──────────────────────────────────────────
    Terminal,
    Hall,
    CheckIn,
    SecurityCheckpoint,
    Customs,
    BoardingLounge,
    Shop,
    Restaurant,

    // ── Accès ─────────────────────────────────────────────────────────────
    Parking,
    RoadAccess,
    BusStop,
    TaxiZone,
}
